coverage:
  status:
    project:
      default:
        # basic
        target: auto
        threshold: 0%
        # advanced settings
        if_ci_failed: error #success, failure, error, ignore
        informational: false
    patch:
      default:
        # basic
        target: auto
        threshold: 0%
        # advanced settings
        if_ci_failed: error
        informational: false